<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1.0" />
  <title>Splash – Nelogica Cloud</title>
  <style>
    * { margin:0; padding:0; box-sizing:border-box; }
    body{
      height:100vh; display:flex; align-items:center; justify-content:center;
      background:linear-gradient(135deg,#0a0e14,#1f2633);
      font-family:'Segoe UI',Tahoma,Verdana,sans-serif;
      color:#00a1f6; overflow:hidden;
    }
    .container{text-align:center;animation:fadeIn 1s ease-out forwards;}
    .logo{width:160px;animation:scaleIn 1.2s ease-out forwards;
          filter:drop-shadow(0 0 8px rgba(0,162,246,.6));}
    .loading{margin-top:1rem;font-size:1.25rem;position:relative;}
    .loading::after{
      content:'';display:inline-block;width:1ch;
      animation:dots 1s steps(3,end) infinite;
    }
    .skip-btn{
      position:absolute;top:1rem;right:1rem;
      background:transparent;border:1px solid #00a1f6;color:#00a1f6;
      padding:.4rem .8rem;border-radius:4px;cursor:pointer;font-size:.85rem;
      transition:background .2s,color .2s;
    }
    .skip-btn:hover{background:#00a1f6;color:#111;}

    @keyframes fadeIn{from{opacity:0}to{opacity:1}}
    @keyframes scaleIn{0%{transform:scale(.5);opacity:0}
                       100%{transform:scale(1);opacity:1}}
    @keyframes dots{
      0%,20%{content:''}40%{content:'.'}60%{content:'..'}80%,100%{content:'...'}
    }
  </style>
</head>
<body>
  <button class="skip-btn" onclick="go()">Pular →</button>

  <div class="container">
    <img src="/View_WEB_ID-03_D.png" alt="Nelogica Logo" class="logo">
    <div class="loading">Carregando Ambiente</div>
  </div>

  <script>
    /**
     * Decide o destino:
     *  - Se o servidor informou que o usuário já está logado
     *    (jaLogado=true), vai direto para /usuarios.
     *  - Caso contrário, envia para /login.
     *
     *  baseUrl cobre implantações em sub-path.
     */
    const base   = "<%= baseUrl %>";
    const target =
      <%= jaLogado ? "'/usuarios'" : "'/login'" %>;

    function go() {
      // se a app estiver em sub-pasta, baseUrl contém o prefixo, ex: '/app'
      window.location.href = base + target;
    }

    // redireciona após 3 s
    setTimeout(go, 3000);
  </script>
</body>
</html>
